.next/
next-env.d.ts
tsconfig.tsbuildinfo

# Regenerated on every `docker build`/`buildah build` by
# scripts/stage-cratestack-cbor-for-docker.mjs (see that script + Dockerfile) — never committed.
.docker-stage/

# Local overrides on top of the committed .env defaults — never committed.
.env.local
.env.*.local
