.next/
next-env.d.ts
tsconfig.tsbuildinfo

# Build output of `@serwist/next` (swDest + its worker chunks) — regenerated by every build.
public/sw.js
public/sw.js.map
public/swe-worker-*.js

# Local overrides on top of the committed .env defaults — never committed.
.env.local
.env.*.local
