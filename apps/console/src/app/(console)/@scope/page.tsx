import { OverviewControlsRail } from '../../../containers/overview-controls-rail';

export const dynamic = 'force-dynamic';

/** `/` — the left rail's secondary section: the Overview parameters. */
export default function OverviewScopeSlot() {
  return <OverviewControlsRail />;
}
