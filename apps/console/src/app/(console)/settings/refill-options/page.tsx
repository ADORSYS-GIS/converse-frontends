import { RefillOptionsCentre } from '../../../../containers/refill-options-centre';

/**
 * `/settings/refill-options` — "Refill options policies" (IA v3 phase 3 — the nav row goes live).
 * `force-dynamic` is inherited from `settings/layout.tsx`.
 */
export default function SettingsRefillOptionsRoute() {
  return <RefillOptionsCentre />;
}
