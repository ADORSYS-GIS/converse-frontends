import { TiersCentre } from '../../../../containers/tiers-centre';

/**
 * `/settings/tiers` — "Tier configs." `force-dynamic` is inherited from `settings/layout.tsx`.
 */
export default function SettingsTiersRoute() {
  return <TiersCentre />;
}
