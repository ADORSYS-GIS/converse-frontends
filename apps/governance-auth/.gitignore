# Vite production build output — regenerated by `pnpm --filter governance-auth build:web`, never
# committed. Turbo already treats it as this app's `build:web` output (turbo.json), and the Rust
# side copies `dist/index.html` into its own tree rather than depending on this one.
dist/
