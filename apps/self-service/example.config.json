{
  "backendUrl": "${EXPO_PUBLIC_BACKEND_URL}",
  "apiBasePath": "${EXPO_PUBLIC_API_BASE_PATH}",
  "gatewayUrl": "${EXPO_PUBLIC_GATEWAY_URL}",
  "gatewayBearerToken": "${EXPO_PUBLIC_GATEWAY_TOKEN}",
  "keycloak": {
    "issuer": "${EXPO_PUBLIC_KEYCLOAK_ISSUER}",
    "clientId": "${EXPO_PUBLIC_KEYCLOAK_CLIENT_ID}",
    "scheme": "${EXPO_PUBLIC_KEYCLOAK_SCHEME}",
    "expectedAudience": "${EXPO_PUBLIC_KEYCLOAK_EXPECTED_AUDIENCE}",
    "audienceRequired": "${EXPO_PUBLIC_KEYCLOAK_AUDIENCE_REQUIRED}"
  },
  "usage": {
    "grafanaUrl": "${EXPO_PUBLIC_GRAFANA_URL}",
    "dashboardPath": "${EXPO_PUBLIC_GRAFANA_USAGE_DASHBOARD_PATH}"
  }
}