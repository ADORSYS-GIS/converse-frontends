{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "title": "converse-console values",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "global": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "labels": {
          "type": "object",
          "additionalProperties": true,
          "properties": {
            "app": { "type": "string" },
            "team": { "type": "string" }
          }
        }
      }
    },
    "console": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "controllers": {
          "type": "object",
          "additionalProperties": true,
          "properties": {
            "main": {
              "type": "object",
              "additionalProperties": true,
              "properties": {
                "containers": {
                  "type": "object",
                  "additionalProperties": true,
                  "properties": {
                    "frontend": {
                      "type": "object",
                      "additionalProperties": true,
                      "properties": {
                        "image": {
                          "type": "object",
                          "additionalProperties": true,
                          "properties": {
                            "repository": { "type": "string", "minLength": 1 },
                            "tag": { "type": "string", "minLength": 1 }
                          }
                        }
                      }
                    },
                    "typst-render": {
                      "description": "The Typst PDF renderer sidecar (apps/typst-render, converse-frontends#456) that the console's report export POSTs to on 127.0.0.1:8080. Story ai-helm-values#346. Disabled by default -- the chart can land before the console route that calls it. Enabling this container REQUIRES enabling console.persistence.typst-tmp with it: the container runs with readOnlyRootFilesystem and every render needs a writable /tmp (apps/typst-render/src/render.ts mkdtemps one throwaway directory per request). Helm cannot express that cross-key dependency, so it is stated here, in values.yaml and in README.md instead.",
                      "type": "object",
                      "additionalProperties": true,
                      "properties": {
                        "enabled": { "type": "boolean" },
                        "image": {
                          "type": "object",
                          "additionalProperties": true,
                          "properties": {
                            "repository": { "type": "string", "minLength": 1 },
                            "tag": {
                              "description": "MUST be an explicit published tag or digest. `latest` is rejected outright rather than merely discouraged: this container is a document compiler, so an unpinned reference makes 'how does last month's report render' depend on when a pod happened to restart. That is the exact breakage class pinning exists to prevent, and a schema rejection at helm lint/install time is the only place it can be caught before a cluster picks it up.",
                              "type": "string",
                              "minLength": 1,
                              "not": { "const": "latest" }
                            }
                          }
                        }
                      }
                    }
                  },
                  "required": ["frontend"]
                }
              },
              "required": ["containers"]
            }
          },
          "required": ["main"]
        },
        "configMaps": {
          "type": "object",
          "additionalProperties": true,
          "properties": {
            "console-config": {
              "type": "object",
              "additionalProperties": true,
              "properties": {
                "data": {
                  "type": "object",
                  "additionalProperties": true,
                  "properties": {
                    "config.yaml": {
                      "description": "The full apps/console config.yaml document for this deployment (server.js's config-loader.ts reads it -- see that file's header comment for the {env:VAR} placeholder syntax it supports). A real deployment supplies its own complete document here (its own Keycloak issuer, backend URLs, etc. as plain literals; secrets as {env:VAR} placeholders backed by a mounted Secret) rather than overriding individual fields.",
                      "type": "string",
                      "minLength": 1
                    }
                  },
                  "required": ["config.yaml"]
                }
              },
              "required": ["data"]
            },
            "branding-logo": {
              "description": "issue #368 (Phase H, runtime white-label branding). Optional -- disabled (console.configMaps.branding-logo.enabled: false) by default, unlike console-config. Holds the operator's logo file as base64 binaryData (app-template's own values schema forbids a ConfigMap entry from carrying both data and binaryData in one resource, hence a separate configMap from branding-style below -- see values.yaml's own comment on persistence.branding-logo).",
              "type": "object",
              "additionalProperties": true,
              "properties": {
                "enabled": { "type": "boolean" },
                "binaryData": {
                  "type": "object",
                  "additionalProperties": true,
                  "properties": {
                    "logo.png": {
                      "description": "Base64-encoded logo file, mounted at /tmp/branding/logo.png (console.persistence.branding-logo). Despite the literal key name, any of the extensions apps/console's env.ts accepts (.png/.svg/.jpg/.jpeg/.webp) may be supplied here -- the key is the ConfigMap's, not a format constraint; config.yaml's own branding.logo path decides the actual served Content-Type.",
                      "type": "string"
                    }
                  }
                }
              }
            },
            "branding-logo-light": {
              "description": "Per-theme logos addendum (owner directive 2026-08-31, \"White is for dark themes\"). Optional -- disabled by default, matching branding-logo above. Holds the operator's light-theme (wireframe) logo file as base64 binaryData, the light-theme counterpart to branding-logo (config.yaml's branding.logo doubles as both the default AND dark-theme mark). Only meaningful when branding-logo is ALSO enabled -- apps/console's env.ts fails config parsing on a branding.logoLight-without-branding.logo document.",
              "type": "object",
              "additionalProperties": true,
              "properties": {
                "enabled": { "type": "boolean" },
                "binaryData": {
                  "type": "object",
                  "additionalProperties": true,
                  "properties": {
                    "logo-light.png": {
                      "description": "Base64-encoded light-theme logo file, mounted at /tmp/branding/logo-light.png (console.persistence.branding-logo-light). Despite the literal key name, any of the extensions apps/console's env.ts accepts (.png/.svg/.jpg/.jpeg/.webp) may be supplied here -- the key is the ConfigMap's, not a format constraint; config.yaml's own branding.logoLight path decides the actual served Content-Type.",
                      "type": "string"
                    }
                  }
                }
              }
            },
            "branding-style": {
              "description": "issue #368 (Phase H). Optional -- disabled by default, matching branding-logo above. Holds the operator's daisyUI custom-property override stylesheet as plain text (GET /branding/override.css filters it server-side -- see apps/console/src/server/branding-css-filter.ts).",
              "type": "object",
              "additionalProperties": true,
              "properties": {
                "enabled": { "type": "boolean" },
                "data": {
                  "type": "object",
                  "additionalProperties": true,
                  "properties": {
                    "override.style": {
                      "description": "CSS custom-property overrides (:root / [data-theme=\"...\"] blocks only), mounted at /tmp/branding/override.style (console.persistence.branding-style).",
                      "type": "string"
                    }
                  }
                }
              }
            },
            "report-templates": {
              "description": "Per-route Typst report template overrides (converse-frontends#453). Optional -- disabled by default; with no override the console renders the templates baked into its image. Keys are FLAT: a ConfigMap key cannot contain '/', so the route tree (admin/overview/report.typ) comes from the MOUNT -- either console.persistence.report-templates.items (flat key -> nested path, text-only bundles) or per-file advancedMounts entries with an explicit path + subPath (the shape that also carries binary assets). See README.md, 'The route tree, and why the key name is not the path'.",
              "type": "object",
              "additionalProperties": true,
              "properties": {
                "enabled": { "type": "boolean" },
                "data": {
                  "description": "Typst sources as text, one key per template file. Kept as data (not binaryData) so the .typ stays readable and diffable in the values file; binary companions go in report-template-assets, because app-template's own values schema forbids one ConfigMap entry from carrying both.",
                  "type": "object",
                  "additionalProperties": { "type": "string" }
                }
              }
            },
            "report-template-assets": {
              "description": "Binary files that ship WITH a report template -- a customer logo, a watermark, a typeface (owner feedback 2026-09-03, 'custom templates WITH embedded logos in prod'). The console sends every non-.typ file sitting beside the resolved report.typ to the renderer as an asset keyed by its path relative to that directory, so a template draws it with image(\"logo.png\") -- no leading slash, because a per-route template compiles as main.typ AT the render root. A separate ConfigMap from report-templates for the same reason branding-logo is separate from branding-style: app-template forbids one entry from carrying both data and binaryData, and a PNG cannot live in data. Requires console.persistence.report-template-assets with one advancedMounts entry per file.",
              "type": "object",
              "additionalProperties": true,
              "properties": {
                "enabled": { "type": "boolean" },
                "binaryData": {
                  "description": "Base64-encoded files, one key per asset. Keys are flat; the mount decides where each lands in the route tree.",
                  "type": "object",
                  "additionalProperties": { "type": "string" }
                }
              }
            }
          },
          "required": ["console-config"]
        }
      },
      "required": ["controllers", "configMaps"]
    }
  },
  "required": ["console"]
}
