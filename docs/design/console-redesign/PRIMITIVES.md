# Primitive migration map — `packages/ui-web`

Practical companion to [ADR 0010](../../adr/0010-ui-primitive-stack-and-theming.md). For every
component that exists in `packages/ui-web/src` today, this says **what it becomes** under the new
primitive stack, and which phase does it.

**2026-08-30 note**: the shell rows below (`console-shell`, `console-header`, `rail-panel`,
`nav-spine`, `sub-nav`) predate the [ADR 0012](../../adr/0012-console-visual-revamp.md) shell
revamp — read them beside that ADR's D1/D3, not in place of it. `console-header` and `rail-panel`
are deleted outright (no successor row; see ADR 0012 "Consequences"); `console-shell` is now the
two-column grid `lib/shell-grid.ts` owns; `nav-spine`/`sub-nav` closed their Base UI gap onto
`navigation-menu`, not the `daisy menu`/`tabs` this table originally predicted. The daisyUI ×
Base UI **primitive-selection method** (daisy class → Base UI behaviour → CVA only for a real
multi-axis variant set) is unchanged — only the shell rows' own predictions were wrong in
hindsight, corrected below rather than left to mislead a future reader.

Implementation agents should be pointed at **rows of this table**, not at the ADR.

Legend for the _Outcome_ column:

- **keep** — no library swap. Tokens/classes may change in phase 1; the component stays ours.
- **class swap** — same React structure, hand-written Tailwind replaced by a daisyUI component
  class (+ utilities for what daisy does not cover).
- **rebuild** — the behaviour moves to a Base UI primitive; our hand-written a11y code is deleted.
- **new** — does not exist yet.

Order of reach, from [ADR 0010](../../adr/0010-ui-primitive-stack-and-theming.md) Decision 4:
daisy class → Base UI behaviour → CVA only if a real multi-axis variant set survives → never a
hand-written focus trap or roving tabindex.

---

## Shell

| Component         | Outcome              | Becomes                     | Phase | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ----------------- | -------------------- | --------------------------- | ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `console-shell`   | keep                 | —                           | 1→4   | Was a three-column flex shell with sticky rails; is now `lib/shell-grid.ts`'s two-column grid (`ConsoleSidebar` + one fluid content column, `min-w-0`). daisy `drawer` is still **not** used — it is a CSS grid sidebar, incompatible with the sidebar's own sticky/independently-scrollable contract. **NO UPSTREAM** (`scripts/base-ui-adoption.ts` records `null`): a layout band around opaque children has no primitive to delegate to.                                              |
| `console-sidebar` | **new**              | —                           | 4     | The persistent 240px `md`+ column: brand, workspace switcher, `NavSpine` (sidebar layout), footer stack. Also renders the mobile bottom-nav dock from the same `groups` prop. Replaces `console-header` + `rail-panel` together — there is no successor row for either, they are deleted (ADR 0012). **NO UPSTREAM.**                                                                                                                                                                     |
| `console-top-bar` | **new**              | —                           | 4     | The `<md` replacement for the sidebar: brand, compact switcher, `⌘K` trigger, identity — a 48px sticky band. **NO UPSTREAM**, same reasoning as `console-shell`.                                                                                                                                                                                                                                                                                                                          |
| `page-header`     | **new**              | —                           | 1     | Every screen's opening block: title/subtitle/controls/action. A static composition of type roles and slots — **NO UPSTREAM**.                                                                                                                                                                                                                                                                                                                                                             |
| `nav-spine`       | **rebuild** — CLOSED | Base UI **navigation-menu** | done  | Closed 2026-08-30, not the `daisy menu` class swap this row originally predicted: `Root`/`List`/`Item`/`Link` (popup half omitted) give `active` → `aria-current="page"` for free, plus orientation-aware arrow navigation. The old Admin-group `ROLE` marker + its own `raised` rule are gone — a role-gated group is simply included or omitted from the `groups` array now (ADR 0012).                                                                                                 |
| `sub-nav`         | **rebuild** — CLOSED | Base UI **navigation-menu** | done  | Closed alongside `nav-spine`, same primitive — corrects this row's original `Tabs` prediction. Renders either the vertical rail-item layout or a horizontal text-tab row (Settings' Account/Projects switch, Attio pattern). Counts stay in the label text, never a badge.                                                                                                                                                                                                                |
| `detail-sheet`    | **new**              | Base UI **Dialog**          | 4     | 420px fixed right panel — header/body/optional footer. The one surface for row detail (project detail, refill review), replacing the persistent-right-rail retarget mechanic this table's pre-revamp rows assumed. Same Dialog idiom every other console dialog already uses.                                                                                                                                                                                                             |
| `bottom-sheet`    | **rebuild**          | Base UI **Drawer**          | done  | Owner decision 2026-08-29, superseding ADR 0010 D2's `vaul` (which was written when Base UI shipped no drawer; 1.7.0 does). `vaul` is removed from the tree — gone from the console path entirely, not just this component. A tier class goes on `Drawer.Portal`, never on the backdrop and panel — a modal portal also holds Floating UI's unclassable full-screen press-absorber. Base UI ships no grab-bar part, so `sheet-handle` is ours, which is what finally let its paint apply. |

## Forms and actions

| Component              | Outcome                                     | Becomes                                                                                   | Phase | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ---------------------- | ------------------------------------------- | ----------------------------------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `button`               | class swap                                  | daisy `btn` (+ `btn-primary`, `btn-secondary`, `btn-ghost`, `btn-sm`/`-md`, `btn-square`) | done  | **`cva.ts` deleted, and the `!` overrides with it — every class this component writes is now daisy's.** The 30/34 heights, the mono 400 type, the focus ring, a `btn-secondary` that draws a `--line` border instead of filling with `--color-secondary`, and the 16px glyph pin on `btn-square` are `@utility` blocks in `theme.css`, not per-variant utilities. `btn-primary` reserved for the one signal action per view; text on the fill is `primary-content`, not `ink` (ADR 0010 D3b a11y finding).                                                                                                                                                                                        |
| `field`                | **rebuild**                                 | Base UI **Field** / **Fieldset** + daisy `input`, `textarea`, `fieldset`, `label`         | done  | Base UI Field owns label association, description and validation state. daisy `input`/`textarea` are the control and `fieldset`/`label` are the two layouts (stacked / side-by-side) — the same pair `select-field`, `scope-select` and `date-range-field` now wear, so the control treatment is ONE `@utility input` block in `theme.css` rather than four copies. `validator`/`validator-hint` stay **unused**: error reuses `--signal`, so a `--color-error` token would only alias `primary`; the border moves on `aria-invalid`. `fieldControlClassName` is the literal string `input`.                                                                                                      |
| `scope-select`         | **rebuild** — CLOSED                        | `SelectField` ×2 (composition, not a second Base UI **Select** import)                    | done  | Kills the native `<select> + appearance-none` pair — the option list becomes themeable and gets keyboard/typeahead for free. Cascade reset (account change clears project) is our logic and stays. Corrects this row's original "Base UI Select ×2": unify-select (issue #368) found this held its own near-copy of `select-field`'s `Select.Root`-to-`Select.Popup` tree, at a shorter, non-palette-matching row height; it composes `SelectField` now, so the Base UI import lives in exactly one place (`base-ui-adoption.test.ts`'s `EXPECTED['scope-select']` is `null` accordingly — a composition of an already-delegating component, the same shape `budget-hero`'s own entry documents). |
| `segmented-control`    | **rebuild**                                 | Base UI **Toggle Group** + daisy `tabs`/`tab`/`tab-active`                                | done  | **Delete** the hand-written roving `tabIndex` and the `ArrowRight/Left/Up/Down/Home/End` switch, and the manual `radiogroup`/`radio` roles. daisy's tab vocabulary is the paint: `tab` brings the height, padding, centring and `position: relative`; the `--line` divider geometry, the `chrome`/`subtle` rest state, the `raised`/`ink` active state and the 2px `primary` underline (a `::after`, not a rendered span) are `@utility` blocks in `theme.css`. This is `tabs`' ONLY consumer — `sub-nav` deliberately took `menu` instead, being route links rather than a switch.                                                                                                               |
| `typed-confirm-dialog` | **rebuild**                                 | Base UI **Alert Dialog**                                                                  | 2     | **Delete** the `querySelectorAll` focus trap, the `Escape` keydown listener, the manual `aria-modal`/`aria-labelledby`/`aria-describedby` wiring and the manual initial focus. Gains scroll lock, background inert, and focus restore. The typed-name gate (`typed === objectName`) is product logic and stays. daisy `modal-box` is **not** used — Base UI portals its own popup.                                                                                                                                                                                                                                                                                                                |
| `confirm-dialog`       | **new** (issue #445)                        | Base UI **Alert Dialog**                                                                  | done  | The plain yes/no sibling of `typed-confirm-dialog`, added for "Start from example policy" overwriting an unsaved draft. Same five `lib/dialog.ts` class strings, so the two panels cannot drift; no typed-name gate, and initial focus lands on Cancel rather than an input. Use the typed gate instead whenever the loss survives the tab closing.                                                                                                                                                                                                                                                                                                                                               |
| `row-action-group`     | class swap → **rebuild** where it overflows | daisy `join` inline; Base UI **Menu** for overflow                                        | 2     | Row-scoped actions stay in the row (ADR 0008 boundary clarification). `join`/`join-item` groups the inline ghost buttons; a Base UI Menu appears only when a row has more actions than fit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `secret-reveal`        | keep + class swap                           | daisy `input input-ghost` + `btn btn-ghost` + `kbd`                                       | 2     | One-time secret strip. Copy affordance keeps its own `navigator.clipboard` logic; the "copied" acknowledgement stays a mono status line, never a toast.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `report-export-panel`  | class swap + **rebuild** (toggles)          | Base UI **Switch** + daisy `toggle`; `select`/`input` for params                          | 2→4   | `ReportExportDialog` hosts it inside a `Dialog`, not a right rail (ADR 0012 D7 — the rail this row assumed is deleted). Format choice is a Base UI Radio group or Toggle Group; include-toggles become Switch + `toggle`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `review-detail-panel`  | class swap                                  | daisy `btn`, `table table-xs`, `fieldset`                                                 | 2→4   | `BottomSheet` content on `/admin/refills-queue`, at every tier — no `/admin/*` or `/settings/*` route has a right rail at any tier to host it instead (ADR 0013 D2; `DetailSheet`, the surface this row originally assumed, is deleted). Decision buttons: approve = `btn-primary` (the panel's one signal action), reject = `btn btn-ghost`. History rows use the `table` class at `table-xs`.                                                                                                                                                                                                                                                                                                   |
| —                      | **new**                                     | `command-palette` — **cmdk**                                                              | 3     | `⌘K`/`Ctrl-K`. Page jumps, account/project scope switch, rail-owned actions. `surface` panel, radius 2, mono, no shadow; `kbd` classes for shortcut hints. Brings `@radix-ui/react-dialog` transitively — that stays transitive.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |

## Data display

| Component                     | Outcome     | Becomes                                                   | Phase | Notes                                                                                                                                                                                                                                                                                                                                                                                      |
| ----------------------------- | ----------- | --------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `card`                        | **new**     | —                                                         | 4     | The console's default zone container (ADR 0012 D3, supersedes this table's own "not adopted" `card` row below). A `base-200` fill, a `border` hairline at `--radius-box`, 1.25rem inset, optional `.card-head`. **NO UPSTREAM** — a padded panel with no interactive state of its own has nothing for Base UI to own.                                                                      |
| `empty-state`                 | **new**     | —                                                         | 4     | First-run empty (ADR 0012 D6): centred column, headline/explainer/action, inside a `Card` or standing alone. **NO UPSTREAM** — a static composition of two type roles and a slot.                                                                                                                                                                                                          |
| `pagination`                  | **new**     | daisy `btn btn-ghost` via `Button`                        | 4     | Caption + Previous/Next composing the already-rebuilt `Button`, not a second control treatment. Renders nothing when neither direction is wired — no pager row of two dead disabled buttons.                                                                                                                                                                                               |
| `ledger-table`                | class swap  | daisy `table` (+ `table-xs`/`table-sm`, `table-pin-rows`) | 2     | Generic typed table stays generic — daisy is CSS only, so columns/sorting/selection are untouched. Wrapper keeps `overflow-x-auto` (daisy's own table rule says the same). Hairlines resolve to `base-300`/`raised`. Zebra striping is **not** used. Row actions (`RowActionGroup`) are always visible, not hover-only, since the 2026-08-30 revamp — hover-only is keyboard-invisible.    |
| `stat-card`                   | keep        | —                                                         | 1     | daisy `stats`/`stat` imposes its own padding, dividers and horizontal grouping; our panel with a mono metric is tighter. Stays self-panelled (its own `surface` fill) even when a `Card` also wraps the row it sits in — not worth the fight, but reconsider if a future card grows a `stat-figure`.                                                                                       |
| `budget-hero`                 | keep        | —                                                         | 1     | Composition of `Meter` + numerals; nothing daisy adds.                                                                                                                                                                                                                                                                                                                                     |
| `meter`                       | **Base UI** | `@base-ui/react/meter` + our 4px square visual            | done  | daisy `progress` stays rejected (rounded 8px, animated). The VISUAL is contract-locked (ADR 0008) and stays ours; the SEMANTICS are Base UI's `Meter.Root/Label/Track/Indicator/Value`. Corrected 2026-08-29 — this row said "keep" while the hand-rolled version shipped `aria-valuenow="600"` against `aria-valuemax="500"`, an invalid range on the console's most load-bearing signal. |
| `sparkline`                   | keep        | —                                                         | 1     | 81×26 unlabelled `<svg>`. No library.                                                                                                                                                                                                                                                                                                                                                      |
| `status-text`                 | keep        | —                                                         | 1     | **Do not** adopt daisy `status` or `badge`. Status is text, never a pill (ADR 0008). Only the colour tokens change.                                                                                                                                                                                                                                                                        |
| `row` deltas (in `stat-card`) | keep        | —                                                         | 1     | Direction is glyph + wording in greys; never green/red. Unchanged by daisy's `success`/`error` colours, which we do not use for deltas.                                                                                                                                                                                                                                                    |

## Charts

| Component            | Outcome               | Becomes                                                              | Phase | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| -------------------- | --------------------- | -------------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `chart-tooltip`      | **rebuild**           | **Floating UI** — `useFloating` + `useClientPoint` + virtual element | 3     | **Delete** `left = x - width/2` clamping, `estimatedHeight = 16 + (title?16:0) + rows.length*18`, and the `containerWidth` prop. Virtual element's `contextElement` is the chart `<svg>`; `flip`/`shift` middleware handles viewport edges; floating element keeps `pointer-events: none`. Snap-to-datum passes explicit `x`/`y` to `useClientPoint`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `chart-axis`         | keep                  | —                                                                    | 1/4   | DOM `<svg>` ticks. Colours move off hex literals to theme variables; light ramp per ADR 0010 D5.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `chart-legend`       | keep                  | —                                                                    | 1/4   | Same.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `spend-series-chart` | keep + tooltip rewire | —                                                                    | 3     | d3 math from `chart-core` verbatim (ADR 0009 D5). Consumes the rebuilt tooltip.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `histogram-chart`    | keep + tooltip rewire | —                                                                    | 3     | Same.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `latency-ridgeline`  | keep + tooltip rewire | —                                                                    | 3     | Same; shared-bin behaviour unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `donut-chart`        | **rebuilt as a RING** | `chart-core`'s `donutGeometry`/`layoutDonutArcs`/`collapseDonutTail` | done  | **Rewritten 2026-09-02** (owner ruling, ADR 0015 D2: "rings allowed, filled disks never"). Superseded twice — the 2026-08-24 original was deleted on 2026-08-29 for `ShareBar` (twenty indistinguishable greys, sub-pixel slivers on a real 99/1/0.4 split), and what returned is a different chart, not a revival. **The hole is an invariant, not a prop**: `donutGeometry` clamps `innerRadiusRatio` into `[MIN_INNER_RADIUS_RATIO, MAX_INNER_RADIUS_RATIO]` = `[0.35, 0.85]` for every input including a non-finite one, so a filled disk is unreachable through the API. Top-N + one summed `Other (N)` wedge (default 6), so the slivers cannot recur; the formatted total lives in the hole (`centreMetric`/`centreLabel`) — which is what a disk has nowhere to put. **No `ChartLegend`** (owner ruling 2026-08-31, values on hover): the shared Floating-UI `ChartTooltip` carries every value, and the old legend list is gone. Still enforces ADR 0008 D6's "orange at most once". Every wedge is its own focusable `role="button"` `<path>` (native tab order) — except under `static`, the print/export mode, which strips every interaction prop and draws the hole's contents as SVG `<text>`. Sanctioned use: exactly the three `model-distribution-*` panels on `/admin/usage`. |
| `chart-tokens.ts`    | **rewrite**           | theme-variable reads                                                 | 1     | The sanctioned hex exception (ADR 0010 D3c): `SPEC_GREY_RAMP`, `SPEC_GRID`, `SPEC_BASELINE`, `SPEC_TEXT_*`, `SPEC_SURFACE`, `SPEC_FLOOR` become theme-resolved. `specSeriesColor`'s rank/accent **behaviour** is unchanged — only which grey a rank resolves to.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

## States

| Component         | Outcome    | Becomes                        | Phase | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ----------------- | ---------- | ------------------------------ | ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `inline-status`   | keep       | —                              | 1     | Empty state is an inline mono status line above still-rendered structure. daisy `alert` is explicitly **not** adopted — it is a bordered, iconed, rounded box.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `error-line`      | keep       | —                              | 1     | `primary` mono line + inline `Retry` ghost.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `skeleton-row`    | class swap | daisy `skeleton` + `h-*`/`w-*` | done  | daisy's `skeleton` supplies the `raised` block. **Its shimmer must be suppressed** — ADR 0008 bans shimmer. `@utility skeleton { … }` (daisy's documented "change a component in CSS" mechanism) fixes it once, centrally. **`animation: none` alone is NOT enough** (corrected 2026-08-29, on first real use of the class): daisy _paints_ the shimmer as a `background-image` gradient at `background-size: 200%` and the animation only _slides_ it, so stopping the animation freezes a static light band across every loading block. `background-image: none` is the declaration that restores the flat `raised` block; `will-change: auto` goes with it, since the hint exists only for the now-absent animation. |
| `skeleton-metric` | class swap | same                           | 2     | Same, matching the metric's final geometry.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

## Screen sections

The screen sections in `packages/ui-web/src/sections/` — the zone-level compositions a route
assembles (stat rows, dashboards, ledgers, rail sections) — stay **pure presentational components
with typed props**; no library changes their contract.

> Superseded naming: this section used to say "page views", naming five `*Page` monoliths under
> `src/pages/`. Those are gone (owner correction, console-ui skill § _Composition_): the library
> exports sections, the shell is mounted once in `apps/console`'s persistent layout, and full-page
> compositions live only in Storybook (`src/pages-stories/`) and in the console's own routes. The
> phase table below applies per section, and per page story, rather than per page component.

| Phase | Work on every section                                                                                               |
| ----- | ------------------------------------------------------------------------------------------------------------------- |
| 1     | Renders unchanged under `data-theme="black"`; no hex literals remain.                                               |
| 2     | Composes the rebuilt form/dialog/menu components; prop APIs preserved where possible so section tests do not churn. |
| 3     | `⌘K` palette reachable from every screen; charts use the rebuilt tooltip.                                           |
| 4     | **A light story variant per page**, plus the existing mobile story, both `addon-a11y`-clean.                        |

The `refine-mock/` harness is unaffected — it drives page views through refine hooks and does not
touch styling.

| Section       | Outcome | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------- | ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `spend-share` | **new** | "Spend — share by project" (owner brief 2026-08-24). Heading + total + status row over a `ShareBar` — a 100%-stacked bar and a ranked list, NOT a donut (replaced 2026-08-29; see README §2.4a for why — that replacement stands for THIS zone, but it is no longer a blanket ban on arcs: ADR 0015 D2 allows rings, and bans filled disks, console-wide). Placed on Overview directly below the SPEND time series, above the LATENCY/BUDGET row. Fed from the same per-project totals `spend-dashboard/fixtures.ts`'s `overviewSpendSeries` already plots, and shares the Overview page's `selectedSeriesKey` state with the SPEND chart. The zone went from ~330px tall to ~90px in the swap. |

---

## What is explicitly _not_ adopted from daisyUI

Recorded so it is not re-litigated per PR. Each of these is a daisy component that looks like a
match but violates a locked ADR 0008/ADR 0012 rule — except `card`, marked below, which is
recorded here for the opposite reason: the rule it used to violate was reversed.

| daisy component                     | Why not                                                                                                                                                                                                                                                                                                                                                             |
| ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `badge`                             | Counts go in tab labels; status is text. No pills.                                                                                                                                                                                                                                                                                                                  |
| `alert`                             | Bordered, iconed, rounded box — empty/error states are inline mono lines.                                                                                                                                                                                                                                                                                           |
| `card`                              | **Reversed 2026-08-30** — cards ARE now the default zone container (ADR 0012 D3; `card`/`empty-state` in this table above). Daisy's own `card` class is still not adopted for it: `console-card`'s hairline-border + `--radius-box` contract and its `.card-head` hook are a one-block `@utility`, cheaper than fighting daisy's own card padding/divider defaults. |
| `drawer`                            | CSS-grid sidebar; incompatible with the sidebar's own sticky/independently-scrollable contract. Drawers are Base UI's `drawer`.                                                                                                                                                                                                                                     |
| `progress` / `radial-progress`      | Rounded, animated; `Meter` is a 4px square track.                                                                                                                                                                                                                                                                                                                   |
| `tooltip` (`data-tip`)              | CSS-only, element-anchored; chart tooltips need a virtual point anchor.                                                                                                                                                                                                                                                                                             |
| `stats` / `stat`                    | Imposes its own padding and dividers; our stat card is tighter and mockup-locked.                                                                                                                                                                                                                                                                                   |
| `modal`                             | Base UI Alert Dialog owns dialog behaviour; daisy's `modal-box` would double up.                                                                                                                                                                                                                                                                                    |
| `glass`, `--depth: 1`, `--noise: 1` | No shadows, no gradients, no grain. Both themes pin `0`.                                                                                                                                                                                                                                                                                                            |
| `table-zebra`                       | Row separation is a `raised` hairline, not alternating fills.                                                                                                                                                                                                                                                                                                       |
