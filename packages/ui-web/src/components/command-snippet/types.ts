export type CommandSnippetProps = {
  /** The read-only shell command shown and copied. */
  command: string;
  /** Optional label above the command, e.g. "Stream logs". */
  label?: string;
  className?: string;
};
