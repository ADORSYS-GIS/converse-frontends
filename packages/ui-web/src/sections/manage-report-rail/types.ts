import type { ReportExportPanelProps } from '../../components/report-export-panel';

/**
 * The MONTHLY REPORT rail section is exactly the `ReportExportPanel` primitive plus its rail
 * identity (`MANAGE_REPORT_RAIL_LABEL`) — composed, never re-implemented.
 */
export type ManageReportRailProps = ReportExportPanelProps;
