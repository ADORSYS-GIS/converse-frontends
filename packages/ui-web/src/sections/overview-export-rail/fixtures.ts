// overview.svg's EXPORT caption, moved here from the deleted `pages/overview` story props.

export const overviewExportCaption = 'Full monthly report lives in Manage.';

/**
 * Matches `apps/console`'s real disabled-reason caption (`overview-rail.tsx`/
 * `overview-centre.tsx`) — the CSV export route doesn't exist yet (console-ui#324, tracked
 * separately as `#308`).
 */
export const overviewExportUnavailableCaption = "Export isn't available yet.";
